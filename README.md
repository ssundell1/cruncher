```
usage: cruncher.py [-h] [--output-file OUTPUT_FILE] [--suffix SUFFIX] input_string

Generate variations of a string.

positional arguments:
  input_string          The input string for generating variations.

options:
  -h, --help            show this help message and exit
  --output-file OUTPUT_FILE, -o OUTPUT_FILE
                        Output file to save the variations. Default: variations.txt
```